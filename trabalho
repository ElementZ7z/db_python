import sqlite3
conexao =conector.connect("banco.db")
cursor = conexao.cursor( )

nome = int(input('Digite o nome da pessoa'))

nota = str(input('Digite a nota'))

materia = input('Digite a materia')


comando = '''UPDATE Pessoa SET nome = ? WHERE'''
cursor.execute(comando,(nome, nota, materia))

registros = cursor.fetchall()

for registro in registros:
  print("-Conteudo: , registro")

  cursor.close()
  conexao.close()